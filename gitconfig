[user]
    name = Drew Chandler
    email = andrewschandler@gmail.com
[color]
    ui=auto
[alias]
    st = "!echo \"****** HEY ******\nUse your 'g' alias\""
    ci = commit
    br = branch
    co = checkout
    lg = "log --pretty=format:'%C(yellow)%h%Creset %s %Cblue%d%Creset' --graph"
    cr = "log --reverse -p --stat origin/master.."
    crd = "log --reverse -p --stat origin/development.."
    pullall = "!git pull && git submodule update --init --recursive"
[push]
    default = tracking
[core]
    excludesfile = /Users/drew/.gitignore
[rebase]
    autosquash = true
[diff]
	compactionHeuristic = true
